// Temporary stand-in for a logged-in user. Swap references to this for
// FirebaseAuth.instance.currentUser once real auth is wired in (last step).
const currentUserId = 'demo-user-sarah';
const currentUserName = 'Sarah Chen';
